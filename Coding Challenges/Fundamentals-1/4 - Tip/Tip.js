let tip;
const bill = 275;

//*Ternary (conditional) operator
tip = bill <= 300 && bill >=50 ? bill * 0.15 : bill * 0.20;
//if(bill <=300 && bill >=50) then bill*0.15 will occur meaning tip = bill * 0.15
//else tip = bill * 0.20
