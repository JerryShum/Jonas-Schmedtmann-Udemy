     
     /*
     //This is INLINE script
      //When opening the html file, it should create a popup (alert) saying that JS is fun
      let js = "amazing";
     
      console.log(40 + 8 + 23 - 10);


      //Declaring a variable and then storing that associated value in the memory
      let firstName = "Jerry";
      */

      //---------------------------------------------------------------------------------------

      //*I really like this comment extension
      //! This is an alert
      //? Honestly im just using these for colours

     /* let javascriptIsFun = true;

      //? typeof is actually something called an OPERATOR which will return the type of the variable
      console.log(typeof javascriptIsFun);

      //Operators:

      const present = 2022;
      const ageJerry = present - 2003;
      console.log(ageJerry);

      console.log(ageJerry / 2, 2**3);
       //* 2**3 is 2 to the power of 3

       const firstName = 'Jerry';
       const lastName = 'Shum';

       console.log(firstName + ' ' + lastName )
       */


       //---------------------------------------------------------------------------------------

       //? String and Template Literals

      /*
       //Template literals will help format the string by assembling multiple pieces together

       const firstName = 'Jerry';
       const job = 'student';
       const birthYear = 2003;
       const present = 2022;

       const jerry = 'I\'m' + firstName + ', a' + (present - birthYear) + 'yearold' + job + '!';

       console.log(jerry); //This is a very annoying way to format and create a string

       //! To write a template string, use backticks (above tab)
       const jerryNew = `I'm ${firstName}, a ${present - birthYear} yearold ${job}!`;
       //This will automatically format the string into the conventional writing method!
       //Remember to use ${} to insert a variable into the template
       console.log(jerryNew);

       //*Template strings are also used to do multiline strings
       //Can be used instead of \n

       console.log(`To do multiline strings simply press enter
       and it will make multiple
       lines `);

       */

      //---------------------------------------------------------------------------------------

      






