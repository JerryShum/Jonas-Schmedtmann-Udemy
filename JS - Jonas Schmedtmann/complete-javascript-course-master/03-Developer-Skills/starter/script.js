// Remember, we're gonna use strict mode in all scripts now!
"use strict";

//We used the .prettierrc file to configure some prettier settings (single quotes vs double quotes)

//We can now use 'cl' to auto complete console.log (snippets -> global snippets)
console.log('meow');

//? Installing node.js and associated npm live server
//Download node.js
//Use vscode terminal -> node -v = successfully installed
//npm install live-server -g
//Node package manager install (tool) global
//live-server will create a server from node

//! can also use node script.js to display output from console into the terminal